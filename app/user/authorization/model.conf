[request_definition]
r = sub, obj, act

[policy_definition]
p = sub_rule, path, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = eval(p.sub_rule) && keyMatch(r.obj.path, p.path) && r.act == p.act
